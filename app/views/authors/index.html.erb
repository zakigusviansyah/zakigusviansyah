<h1><center> DATA AUTHOR </center></h1>
<table>
    <tr>
        <td><h3>Name</h3</td>
    </tr>
    <% @authors.each do |author| %>
    <tr>
        <td><%= author.name %></td>
        <td><%= link_to "Show", author_path(author)%> | <%= link_to "Edit", edit_author_path(author)%> | 
        <%= link_to "Delete", author_path(author),method: :delete,data: {confirm: 'Are you sure?'}%> </td>
    </tr>
    <% end %> 
    <tr>
    </tr>
</table>
<%= link_to "New Author", new_author_path %>